- if @store.store_managers.present?
  - @store.store_managers.each do |staff|
    tr 
      td.collapsing= staff.id
      td= staff.name
      td.collapsing= staff.cpf
      td.collapsing= staff.job
      td.collapsing
        = link_to edit_store_manager_path(@store, staff), remote: true, class: 'ui button green tiny icon' do
          i.icon.pencil
        = link_to store_manager_path(@store, staff), method: :delete, remote: true, class: 'ui button red tiny icon', data: { confirm: t(:confirm)} do
          i.icon.trash
- else
  tr
    td colspan=6
      i Nenhum gerente cadastrado.